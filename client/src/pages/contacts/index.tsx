import React from "react";
import DefaultLayout from "../../layout/defaultLayout";

const Contacts = () => {
  return(
    <DefaultLayout>
      <div>Contacts</div>
    </DefaultLayout>
  )
}

export default Contacts