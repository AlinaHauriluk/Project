import type {FC} from "react";
import DefaultLayout from "../../layout/defaultLayout";

const Profile:FC = () => (
  <DefaultLayout>
    <div>Profile</div>
  </DefaultLayout>
);

export default Profile;
